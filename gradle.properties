# Increases the amount of memory Gradle can use and optimizes the GC
	# -XX:+UseZGC
	org.gradle.jvmargs=-Xmx4G
# Helps with build times
	org.gradle.parallel=true
# Caches Dependencies for faster build times.
    org.gradle.caching=false

# Fabric Properties
	# Get new versions at https://fabricmc.net/develop
	minecraft_version=26.3
	fabric_loader_version=0.19.5
	min_fabric_loader_version=0.19.0

# Mod Properties
    mod_id = frozenlib
    mod_name = FrozenLib
    mod_author = FrozenBlock
    mod_version = 3.0
    maven_group = net.frozenblock
    archives_base_name = FrozenLib
    subproject_prefix = flib
    java_version = 25
    mod_url = https://modrinth.com/mod/frozenlib
    source_url = https://github.com/FrozenBlock/FrozenLib
    issues_url = https://github.com/FrozenBlock/FrozenLib/issues
    license = GPL-3.0
    repository = FrozenBlock/FrozenLib

# Publishing
	modrinth_project_id = 9KawNmQc
	curseforge_project_id = 709773

# Dependencies
	fabric_api_version=0.160.7+26.3
    fabric_kotlin_version=1.14.1+kotlin.2.4.20
    toml4j_version=0.7.2
    jankson_version=1.2.3
    xjs_data_version=0.14
    xjs_compat_version=0.13
    fresult_version=3.1

# External Mods
	modmenu_version=21.0.0-beta.1
	cloth_config_version=26.3.158

# NeoForge Properties (verify versions against the snapshot maven)
    neoforge_version=26.3.0.3-beta
    neoforge_loader_version_range=[4,)
    kotlinforforge_version=6.2.0

# Optional: set to a PR/snapshot NeoForge maven URL, or leave blank to skip
    neoforge_snapshot_maven=
