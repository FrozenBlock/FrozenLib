before_install:
  - wget https://github.com/sormuras/bach/raw/releases/11/install-jdk.sh
  - source install-jdk.sh --feature 17
  - mv build.gradle temp.gradle
  - gradle wrapper --gradle-version 7.5.1 --no-daemon --stacktrace
  - mv temp.gradle build.gradle

jdk:
  - openjdk17